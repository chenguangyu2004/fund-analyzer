# 基金分析App — AI多因子透视诊断

基于 Python + Flask 的基金智能分析工具，集成 **AI多因子透视诊断模型**，对基金进行五维度量化评分 + DeepSeek AI 深度解读。

**版本**: v7.2.0 (2026-06-24)

---

## 快速开始

```bash
# 1. 安装依赖
pip install -r requirements.txt

# 2. 配置 AI（可选，推荐）
echo DEEPSEEK_API_KEY=sk-your-key > .env

# 3. 启动
python app.py
# 或双击 start.bat
```

浏览器访问 **http://127.0.0.1:5000**

> 无 API Key 也能使用基础分析（净值、盈亏、持仓、K线），AI 功能需要 [DeepSeek 密钥](https://platform.deepseek.com/)。

---

## 核心功能

### AI 多因子透视诊断

基于《AI多因子透视诊断模型规格书 v2.0》，五模型并行打分 + 市场环境动态权重 + AI 综合解读：

| 模型 | 权重 | 核心问题 | 关键指标 |
|:---|:---:|:---|:---|
| **M1 个股质量** | 25% | 买的是不是好公司？ | ROE、毛利率、净利增速、OCF/NI、负债率 |
| **M2 估值性价比** | 25% | 好公司现在贵不贵？ | PE分位数、PB分位数、PEG、股息率 |
| **M3 舆情动量** | 20% | 市场认可这些公司吗？ | 新闻情感、分析师评级、资金流向 |
| **M4 行业景气** | 15% | 赛道顺风还是逆风？ | 行业景气度、政策支持、宏观匹配 |
| **M5 经理行为** | 15% | 管钱的人靠谱吗？ | 风格稳定性、调仓质量、业绩可持续 |

权重按市场环境**动态调整**（正常/牛市/熊市/震荡市/高不确定性 5 种模式）。

### 基金基础分析

- **今日收益概览**：侧边栏顶部汇总所有已保存基金的当日盈亏
- 已保存基金列表：每只基金显示**实时净值、涨跌幅、日盈亏**
- 基金基本信息：名称、净值、基金经理、规模、类型
- 盈亏计算：总盈亏、当日盈亏、持仓成本 vs 当前价值
- 历史净值走势 + 沪深300基准对比
- 前十大持仓：实时价格、涨跌幅、行业分布
- 实时估算净值：基于持仓股票价格计算（交易时段）

### ETF 联接基金穿透

ETF联接基金（如 012724、013309）自动穿透到底层 ETF 获取真实持仓：

- **A股 ETF联接** → 穿透到 159xxx/51xxxx 场内 ETF → 显示 ETF 的完整持仓
- **QDII ETF联接** → 穿透到跨境 ETF → 显示港股/美股持仓

> 示例：国泰中证畜牧养殖ETF联接A (012724) 只有 1 只直接持股 → 自动穿透到 159865 养殖ETF → 展示 39 只底层持仓

### 个股详情页

- 实时行情（A股/港股/北交所全覆盖）
- **K线图**（日K/周K/月K，支持A股+港股）
- 财务数据（ROE、EPS、营收、净利润、负债率）
- **个股新闻**（A股：新浪个股页+东方财富公告；港股：新浪个股页+腾讯快讯，点击可跳转原文 ↗）
- **行业龙头股**（同行业龙头 + 实时涨跌，JSON数据+AI自动保鲜）

### AI 对话问答

- 单基金深度问答
- 跨基金对比分析
- 投资组合诊断

---

## 界面设计

### Stripe 风格设计系统

- **浅色画布**：`#f6f9fc` 背景 + 上半部奶油→薰衣草→靛蓝渐变网格
- **深色模式**：一键切换 `body[data-theme="dark"]`，所有变量完整覆盖
- **Inter 300 weight** 排版：负字距展示级标题，`tnum` 表格数字
- **胶囊按钮**：`9999px` 圆角，靛蓝单色 CTA
- **页面边缘星光**：左右两侧 ✦✧ 闪烁 + 流星拖尾动画
- **空状态星空**：保存列表/AI分析区无内容时星星闪烁

---

## 项目结构

```
fund-analyzer/
├── app.py                      # Flask 应用 + API 路由
├── config.py                   # 配置中心（模型/超时/线程/日志级别）
├── logger.py                   # 统一日志（RotatingFileHandler 5MB×3）
├── fund_analyzer.py            # 基金数据（净值/持仓/风险/费率/策略/ETF穿透）
├── stock_analyzer.py           # 股票数据（行情/K线/新闻/龙头/财务）
├── deepseek_analyzer.py        # DeepSeek AI API 封装 + 新闻聚合
├── multifactor_model.py        # M1-M5 评分引擎 + 动态权重
├── requirements.txt            # 项目依赖
├── .env.example                # 环境变量模板
├── data/
│   ├── industry_leaders.json   # 行业龙头股（19行业×119只）
│   ├── industry_benchmarks.json # 行业基准+估值权重+宏观映射+动态权重
│   ├── industry_keywords.json  # 名称关键词→行业映射
│   └── stock_industry_map.json # 股票代码→行业映射
├── static/
│   └── css/
│       └── stripe-theme.css    # Stripe 设计系统（354条CSS规则，浅色+深色）
├── templates/
│   ├── index.html              # 基金分析主页
│   └── stock.html              # 股票详情页
├── tests/
│   └── test_multifactor.py     # 核心评分 15 个单元测试
├── AI多因子透视诊断_模型.xlsx
├── AI多因子透视诊断_模型规格书.md
└── 状态说明.md
```

---

## API 端点

| 端点 | 方法 | 说明 |
|:---|:---|:---|
| `/` | GET | 基金分析主页 |
| `/stock` | GET | 股票详情页 |
| `/api/analyze` | POST | 基金基础分析（信息、盈亏、持仓、历史） |
| `/api/ai-analyze` | POST | **AI 多因子透视诊断**（五模型评分 + AI 解读） |
| `/api/ai-chat` | POST | AI 对话问答（单基金/跨基金/组合） |
| `/api/stock/<code>` | GET | 股票详情（行情、新闻、龙头、财务） |
| `/api/stock/<code>/kline` | GET | 股票K线数据（日/周/月） |
| `/api/market-overview` | GET | 市场概况（四大指数 + 财经新闻） |
| `/api/check-api-key` | GET | 检查 API Key 是否配置 |
| `/api/fund/<code>/benchmark` | GET | 基金净值 + 沪深300 基准对比 |
| `/api/fund/<code>/risk-metrics` | GET | 风险指标（夏普/回撤/波动率） |
| `/api/fund/<code>/fees` | GET | 基金费率结构 |
| `/api/fund/<code>/strategy-analysis` | GET | 量化策略（长期稳定性/下跌预测/上涨预测） |
| `/api/portfolio-analyze` | POST | 投资组合 AI 分析 |

---

## 配置说明

所有参数通过 `.env` 文件或环境变量配置：

| 变量 | 默认值 | 说明 |
|:---|:---|:---|
| `DEEPSEEK_API_KEY` | *(空)* | DeepSeek API 密钥 |
| `DEEPSEEK_MODEL` | `deepseek-v4-pro` | AI 模型选择 |
| `FLASK_HOST` | `127.0.0.1` | 服务器监听地址 |
| `FLASK_PORT` | `5000` | 服务器端口 |
| `LOG_LEVEL` | `INFO` | 日志级别 |
| `AI_MAX_TOKENS` | `8192` | AI 最大输出长度 |
| `REQUEST_TIMEOUT_AI` | `120` | AI 请求超时(秒) |

详见 `.env.example`。

---

## 数据维护

行业龙头数据通过 `data/industry_leaders.json` 管理，**每次启动时后台自动用 AI 扫描变化并追加新龙头**（每天一次，需 API Key）。

也可手动编辑 JSON 文件，重启后生效：

```json
{
  "互联网": [
    {"code": "00700", "name": "腾讯控股"},
    {"code": "09988", "name": "阿里巴巴"}
  ]
}
```

同样可编辑 `data/industry_benchmarks.json` 更新行业基准数据（ROE中位数/PE中位数等）。

---

## 运行测试

```bash
pytest tests/ -v
```

15 个测试覆盖：M1~M5 评分、综合评分、等级转换、市场环境识别。

---

## 更新日志

### v7.2.0 (2026-06-24) — UI 重设计 + ETF 穿透

- 🎨 **Stripe 设计系统**：浅色画布 + 胶囊按钮 + Inter 300 排版 + `tnum` 表格数字 + 渐变网格背景
- 🌙 **深色模式**：一键切换，所有 CSS 变量完整覆盖，图表自适应
- ⭐ **ETF 联接基金穿透**：自动识别并穿透到底层 ETF 获取真实持仓（A股/QDII 均支持）
- 💰 **今日收益概览**：侧边栏顶部汇总所有已保存基金的当日盈亏
- 📊 **已保存基金涨跌显示**：每只基金显示实时净值、涨跌幅、日盈亏
- 🐛 **保存基金名不对码**：修复输入代码未分析就保存导致的名称错配
- 🔗 **新闻直链修复**：`&` 转义导致的 URL 损坏 → 改用 `addEventListener` 绑定
- ✨ **页面边缘星光**：左右两侧星星闪烁 + 流星拖尾动画
- 📐 **CSS 外置**：`static/css/stripe-theme.css` 统一管理（354 条规则）
- 🔢 **A股涨跌颜色**：红涨绿跌（中国惯例），列表/图表统一

### v7.1.0 (2026-05-30) — 架构重构

- 🔧 **统一日志系统**：`logger.py` + RotatingFileHandler，替代 120+ 处散落 `print()`
- ⚙️ **配置中心**：`config.py` 统一管理 API Key / 模型 / 超时 / 线程数 / 日志级别
- 📦 **数据解耦**：行业龙头/基准/映射从硬编码迁移到 4 个 JSON 文件
- 📰 **新闻多源聚合**：A股（新浪个股页+东方财富）、港股（新浪个股页+腾讯快讯）
- 📈 **K线修复**：港股 5 位代码误判为深证 → 优先判断位数
- 🏷 **行业龙头修复**：静态 JSON 优先（准确快速），AI 后台自动保鲜
- 🧪 **测试覆盖**：15 个 pytest 单元测试（M1~M5 + 市场环境识别）

### v7.0.0 (2026-05-23) — AI 多因子透视诊断

- ✨ M1-M5 五模型并行打分引擎
- ✨ 动态权重调整（5 种市场环境）
- ✨ DeepSeek AI 深度解读
- ✨ 20 只重仓股 5 线程并发获取

---

## 数据来源

| 数据 | 来源 |
|:---|:---|
| 基金净值 | 东方财富 / 天天基金网 |
| 基金持仓 | 东方财富基金详情页 |
| ETF持仓穿透 | 东方财富基金详情页 → 查看相关ETF → 场内ETF持仓 |
| 实时估值 | 天天基金网 / 持仓计算 |
| 股票行情 | 腾讯财经 / 新浪财经 |
| K线数据 | 腾讯财经 / 东方财富 |
| 财务数据 | 东方财富数据中心 |
| 个股新闻 | 新浪财经个股页 / 东方财富公告 / 腾讯快讯 |
| 市场环境 | 东方财富 / 新浪财经 |
| AI 分析 | DeepSeek API |

---

## 免责声明

本应用提供的基金分析、评分和建议仅供参考，**不构成投资建议**。

- 定量评分基于公开数据和既定公式计算
- AI 解读由大语言模型生成，可能存在偏差
- 投资决策请结合自身风险承受能力
- 过往业绩不代表未来表现

**投资有风险，入市需谨慎。**
